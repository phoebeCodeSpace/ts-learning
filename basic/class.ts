/**
 * 类
 */
/**
 * public    : 公共,属性对外部可见,默认
 * private   : 私有,只对自己可见,不能在声明它的类的外部访问
 * protected : 受保护的,在派生类可见
 *             构造函数也可以被标记成 protected。 这意味着这个类不能在包含它的类外被实例化，但是能被继承。
 * readonly  : 只读,必须在声明时或构造函数里被初始化
 */